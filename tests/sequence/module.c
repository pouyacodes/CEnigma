#include "module.h"

#define CASES \
    CASE(sequence_pos_first) \
    CASE(sequence_pos_third) \
    CASE(sequence_pos_not_exists) \
    CASE(sequence_reflect_first) \
    CASE(sequence_reflect_last) \
    CASE(sequence_reflect_mid)


#include "../test_skeleton.c"
